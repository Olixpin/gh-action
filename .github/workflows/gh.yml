name: workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: "Display Hello world"
        run: echo "Hello World🌎"
      - name: "Print Good bye"
        run: echo "Done - Bye!"
